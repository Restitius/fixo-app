// Placeholder — built out fully in Sub-phase M-G (profile hub: business,
// pricing, services, service-areas, documents, wallet, payouts, invoices,
// settings all live under this tab as sub-screens).
import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

export default function Profile() {
  return (
    <SafeAreaView className="flex-1 bg-app-bg px-6 pt-4">
      <Text className="text-[22px] font-extrabold text-ink">Profile</Text>
    </SafeAreaView>
  )
}
